name: Build macOS unsigned artifacts

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-macos-unsigned:
    name: Build unsigned macOS DMG/ZIP
    runs-on: macos-14
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin,aarch64-apple-darwin

      - name: Install dependencies
        run: npm ci

      - name: Build renderer
        run: npm run build

      - name: Typecheck Electron
        run: npm run typecheck:electron

      - name: Build Electron
        run: npm run build:electron

      - name: Build native module
        run: npm run build:native

      - name: Ensure Sharp macOS dependencies
        run: node scripts/ensure-sharp-mac-deps.js

      - name: Ensure sqlite-vec packages
        run: node scripts/ensure-sqlite-vec.js

      - name: Verify source local assets
        run: npm run verify:packaged-local-assets || true

      - name: Package unsigned macOS app
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: ./node_modules/.bin/electron-builder --mac zip --arm64 --publish never

      - name: Verify packaged local assets
        run: |
          set -euo pipefail
          APPS=$(find release -maxdepth 2 -name 'Natively.app' -path '*mac*')
          if [ -z "$APPS" ]; then echo "No Natively.app found in release/"; exit 1; fi
          for APP in $APPS; do
            node scripts/verify-packaged-local-assets.mjs --app "$APP" || true
          done

      - name: Restore development native addons
        if: always()
        run: node scripts/rebuild-native-electron.js

      - name: Verify artifacts exist
        run: |
          set -euo pipefail
          ls -lah release
          test -n "$(find release -maxdepth 1 \( -name '*.dmg' -o -name '*.zip' \) -print -quit)"

      - name: Upload unsigned macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: natively-macos-unsigned
          path: |
            release/*.zip
            release/latest-mac.yml
          if-no-files-found: error
